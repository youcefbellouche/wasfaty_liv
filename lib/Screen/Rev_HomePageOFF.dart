import 'package:flutter/material.dart';

class Rev_HomePageOFF extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child : Text('pas de co')
    );
  }
}